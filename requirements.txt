# Pinned set for the prematch dashboard. Superset of the root requirements.txt;
# extras = fastapi/uvicorn/jinja2 (web), rapidfuzz (matcher), pyyaml (aliases),
# pytest (tests).
httpx
playwright
beautifulsoup4
curl_cffi      # CB_TRANSPORT=http — browser-free CB postbacks
html5lib       # browser-equivalent HTML repair for raw ASP.NET panels
numpy          # soccer_model — Poisson score matrices, devig, fair-price sheet
fastapi
uvicorn[standard]
jinja2
rapidfuzz
pyyaml
python-dotenv
pytest
